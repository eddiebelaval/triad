# /changelog - Derived Changelog from the Triad

You are computing a **changelog** from the three-document triangle (VISION.md, SPEC.md, BUILDING.md).

Load and follow the skill at: `.claude/skills/triad-derive/SKILL.md` — execute the `/changelog` command section.

## Arguments
$ARGUMENTS

## Quick Reference
- No arguments: changelog since last SPEC reconciliation
- `--project <name>`: target a specific project
- `--days <N>`: override the lookback window
- `--since <date>`: explicit start date
