# /drift - Trajectory Analysis from the Triad

You are computing a **drift analysis** from the three-document triangle (VISION.md, SPEC.md, BUILDING.md).

Load and follow the skill at: `.claude/skills/triad-derive/SKILL.md` — execute the `/drift` command section.

## Arguments
$ARGUMENTS

## Quick Reference
- No arguments: full drift analysis for current project
- `--project <name>`: target a specific project
- `--days <N>`: change the lookback window (default: 30)
